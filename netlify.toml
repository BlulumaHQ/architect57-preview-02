# Netlify build settings for the Architect 57 Vite SPA.
# Redirect rules are the single source of truth in public/_redirects,
# which Vite copies verbatim to dist/_redirects at build time.

[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
